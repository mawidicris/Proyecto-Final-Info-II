#include "puntaje.h"


puntaje::puntaje(QObject *parent)
{/*
    punt= new QGraphicsTextItem;
    punt->setPlainText("Puntaje: "+QString::number(Puntaje));
    setDefaultTextColor(Qt::black);
    setFont(QFont("times",16));
    scene()->addItem(this);
    */
}
